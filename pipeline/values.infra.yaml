bake:
  environments:
    - infra
deploy:
  environments:
    - infra
  env:
    MONGO_HOST: scratch-infra.cluster-cacdh9v0zkat.us-east-1.docdb.amazonaws.com
    MONGO_DB: people?ssl=true
    MONGO_USER: master
    MONGO_PASSWORD: s3cret
    MONGO_SSL_CA: /app/data/rds.pem
  volumes:
    - path: /app/data/rds.pem
      data: https://s3.amazonaws.com/rds-downloads/rds-combined-ca-bundle.pem
